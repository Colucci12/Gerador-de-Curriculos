# Flask
SECRET_KEY=

# Fernet (chaves Gemini dos usuários)
# Gere com: docker compose run --rm web python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Traefik / Portainer
APP_DOMAIN=
TRAEFIK_NETWORK=traefik
TRAEFIK_ENTRYPOINT=websecure

# Opcional: só CLI local
GOOGLE_API_KEY=
