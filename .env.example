# Sessão Flask (string longa e aleatória)
SECRET_KEY=troque_por_uma_string_secreta_longa

# Fernet — gere com:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Opcional: só para o CLI (script_tailor). A web usa a chave de cada usuário.
GOOGLE_API_KEY=sua_chave_aqui
